[user]
  name = Steven Troxler
  email = steven.troxler@gmail.com
[color]
  ui = auto
[core]
  editor = nvim
  excludesfile = ~/.gitignore_global
[alias]
    # log graph
    lg = log --graph --abbrev-commit --decorate --date=relative\
 --format=format:'%C(bold blue)%h%C(reset) - %C(bold\
 green)(%ar)%C(reset) %C(blue)%s%C(reset) -\
 %an%C(reset)%C(bold blue)%d%C(reset)' --all
[mergetool]
	prompt = false
[merge]
    tool = kdiff3
[mergetool "kdiff3"]
    trustExitCode = false
[diff]
    guitool = kdiff3
[difftool "kdiff3"]
    trustExitCode = false

# circleci local adds this automatically, but it causes
# some issues with cargo
#
#[url "git@github.com:"]
#	insteadOf = https://github.com/

# Some proxy config is useful for me on some machines
# [http]
# 	proxy = https://fwdproxy:8082
# 	proxySSLCert = /var/facebook/credentials/stroxler/x509/stroxler.pem
# 	proxySSLKey = /var/facebook/credentials/stroxler/x509/stroxler.pem
# 
# [credential]
#	helper = cache --timeout=31536000
